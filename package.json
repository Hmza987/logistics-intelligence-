{
  "name": "gcc-logistics-dashboard",
  "version": "1.0.0",
  "private": true,
  "description": "Gulf Logistics Intelligence — live Hormuz/chokepoint data dashboard",
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.100.1",
    "docx": "^9.7.1",
    "dotenv": "^17.4.2",
    "node-fetch": "2.7.0"
  },
  "devDependencies": {
    "exceljs": "^4.4.0",
    "html-validate": "^9.0.0"
  },
  "scripts": {
    "validate": "node --check api/hormuz.js && echo 'Syntax OK'",
    "dev": "npx serve . --listen 3000 --no-clipboard",
    "deploy": "vercel --prod"
  }
}
